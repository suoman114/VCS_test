[pytest]
asyncio_mode = auto
testpaths = tests
; app/api/test_cases.py, app/api/test_runs.py는 API 라우터 파일이지 pytest
; 테스트 모듈이 아니다. testpaths로 tests/만 스캔하지 않으면 파일명이
; test_*.py 패턴과 우연히 일치해 pytest가 오수집을 시도한다(예: test_runs.py의
; test_run_ws 웹소켓 핸들러를 테스트 함수로 오인).
