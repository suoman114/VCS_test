# frontend

React + TypeScript 대시보드. 담당 에이전트: `frontend-dashboard-agent`.

디렉토리 구조는 루트 `CLAUDE.md` §5를 따른다. 아직 코드가 없으며, 구현 시작 시 아래 구조로 채워진다.

```
src/
├── pages/
├── components/
├── api/
└── store/
```
